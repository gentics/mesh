[options="header",cols="10%,10%,10%,70%"]
|======

| Property
| Mandatory 
| Type
| Description


| deleteOther
| false
| boolean
| This flag states that all the existing variants, except the mentioned in &#x60;variants&#x60; field, should be dropped.If no variants are provided along with the flag, all the image manipulation variants are to be dropped.

| variants
| false
| array
| This field contains a set of image manipulation variants to be manipulated upon.

|======
