[options="header",cols="10%,10%,10%,70%"]
|======

| Property
| Mandatory 
| Type
| Description


| availableLanguages
| yes
| object
| Map of languages for which content is available and their publish status.

| breadcrumb
| yes
| array
| List of nodes which construct the breadcrumb. Note that the start node will not be included in the list.

| childrenInfo
| no
| object
| Object which contains information about child elements.

| container
| yes
| boolean
| Flag which indicates whether the node is a container and can contain nested elements.

| created
| yes
| string
| ISO8601 formatted created date string.

| creator
| yes
| object
| User reference of the creator of the element.

| displayField
| no
| string
| Display field name of the node. May not be retured if the node schema has no display field.

| displayName
| no
| string
| Display field value of the node. May not be retured if the node schema has no display field.

| edited
| yes
| string
| ISO8601 formatted edited date string.

| editor
| no
| object
| User reference of the editor of the element.

| fields
| yes
| object
| Dynamic map with fields of the node language specific content.

| language
| no
| string
| ISO 639-1 language tag of the node content.

| languagePaths
| no
| object
| Map of webroot paths per language. This property will only be populated if the resolveLinks query parameter has been set accordingly.

| parentNode
| no
| object
| Reference to the parent node. Note that the root node of the project has no parent node.

| path
| no
| string
| Webroot path to the node content. Will only be provided if the resolveLinks query parameter has been set accordingly.

| permissions.create
| yes
| boolean
| Flag which indicates whether the create permission is granted.

| permissions.delete
| yes
| boolean
| Flag which indicates whether the delete permission is granted.

| permissions.publish
| no
| boolean
| Flag which indicates whether the publish permission is granted.

| permissions.read
| yes
| boolean
| Flag which indicates whether the read permission is granted.

| permissions.readPublished
| no
| boolean
| Flag which indicates whether the read published permission is granted.

| permissions.update
| yes
| boolean
| Flag which indicates whether the update permission is granted.

| project
| yes
| object
| Reference to the project of the node.

| rolePerms
| no
| object
| Permission information for provided role. This property will only be populated if a role query parameter has been specified.

| schema
| yes
| object
| Reference to the schema of the node.

| tags
| yes
| array
| List of tags that were used to tag the node.

| uuid
| yes
| string
| Uuid of the element

| version
| yes
| string
| Version of the node content.

|======
