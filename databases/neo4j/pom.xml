<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>mesh-neo4j</artifactId>
	<packaging>jar</packaging>
	<name>Mesh - Neo4j</name>

	<parent>
		<groupId>com.gentics.mesh</groupId>
		<artifactId>mesh-databases</artifactId>
		<version>0.6.2</version>
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<neo4j.version>2.2.2</neo4j.version>
		<neo4j.cypher.version>2.1.4</neo4j.cypher.version>
		<blueprints-neo4j2-graph.version>2.7.0-SNAPSHOT</blueprints-neo4j2-graph.version>
	</properties>

	<dependencies>

		<!-- Workaround to fix fat-jar jersey issue with neo4j browser -->
		<!-- This is a workaround/hack - We need to specifiy the lucene index dependency. 
			Otherwise the fatjar is not build correctly. -->
		<!-- <dependency> <groupId>com.sun.jersey</groupId> <artifactId>jersey-core</artifactId> 
			<version>1.9</version> </dependency> <dependency> <groupId>org.neo4j</groupId> 
			<artifactId>neo4j-lucene-index</artifactId> <version>${neo4j.version}</version> 
			</dependency> -->

		<!-- Tinkerpop implementation -->
		<dependency>
			<groupId>com.tinkerpop.blueprints</groupId>
			<artifactId>blueprints-neo4j2-graph</artifactId>
			<version>2.7.0-SNAPSHOT</version>
			<exclusions>
				<exclusion>
					<artifactId>neo4j-kernel</artifactId>
					<groupId>org.neo4j</groupId>
				</exclusion>
				<exclusion>
					<artifactId>neo4j-ha</artifactId>
					<groupId>org.neo4j</groupId>
				</exclusion>
				<exclusion>
					<artifactId>neo4j-management</artifactId>
					<groupId>org.neo4j</groupId>
				</exclusion>
				<exclusion>
					<artifactId>neo4j</artifactId>
					<groupId>org.neo4j</groupId>
				</exclusion>
			</exclusions>
		</dependency>


		<dependency>
			<groupId>org.neo4j</groupId>
			<artifactId>neo4j-kernel</artifactId>
			<version>${neo4j.version}</version>
			<classifier>tests</classifier>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.neo4j</groupId>
			<artifactId>neo4j-kernel</artifactId>
			<version>${neo4j.version}</version>
			<type>test-jar</type>
			<scope>compile</scope>
		</dependency>


		<!-- Neo4j -->
		<dependency>
			<groupId>org.neo4j</groupId>
			<artifactId>neo4j-kernel</artifactId>
			<version>${neo4j.version}</version>
		</dependency>
		<dependency>
			<groupId>javax.transaction</groupId>
			<artifactId>jta</artifactId>
			<version>1.1</version>
		</dependency>

		<!-- Neo4j test dependencies -->
		<dependency>
			<groupId>org.neo4j</groupId>
			<artifactId>neo4j-io</artifactId>
			<version>${neo4j.version}</version>
			<classifier>tests</classifier>
			<scope>compile</scope>
		</dependency>

	</dependencies>

	<repositories>
		<repository>
			<id>neo4j</id>
			<url>http://m2.neo4j.org/releases/</url>
			<releases>
				<enabled>true</enabled>
			</releases>
		</repository>
	</repositories>
</project>
