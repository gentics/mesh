Search: An index search integration with Elasticsearch has been refactored to optionally use stateless `search_after` approach, along with the existing stateful `scroll` API. 
A new search config item, `indexSearchMode`, has been added, with the following values allowed `SCROLL` (default) and `SEARCH_AFTER`.