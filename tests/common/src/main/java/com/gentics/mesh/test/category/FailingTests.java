package com.gentics.mesh.test.category;

/**
 * JUnit marker interface for failing tests. A test can fail because it is unstable or because it reveals a still open bug.
 */
public interface FailingTests {

}
