{
  rootNode {
    children(filter: {fields:{content:{slug:{equals:"test2"}}}}) {
      elements {
        # [$.data.rootNode.children.elements[0].isPublished=false]
        isPublished
        ... on content {
          fields {
            # [$.data.rootNode.children.elements[0].fields.content=/test1]
            content(linkType: SHORT)
            # [$.data.rootNode.children.elements[0].fields.teaser=/test1]
            teaser(linkType: SHORT)
    
            # [$.data.rootNode.children.elements[0].fields.contentFull=%CURRENT_API_BASE_PATH%/dummy/webroot/test1?branch=testBranch]
            contentFull: content(linkType: FULL)
            # [$.data.rootNode.children.elements[0].fields.teaserFull=%CURRENT_API_BASE_PATH%/dummy/webroot/test1?branch=testBranch]
            teaserFull: teaser(linkType: FULL)
          }
        }
      }    
    }
  }
}
