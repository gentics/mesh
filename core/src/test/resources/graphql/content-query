{
	content(path:"/Neuigkeiten/2015") {
		# [$.data.content.node.ruContainer=<is-null>]
		node {
			ruContainer: content(language: "ru") {
				version
			}
			content(language: "en") {
				# [$.data.content.node.content.version=1.0]
				version
	
				# [$.data.content.node.content.isDraft=true]
				isDraft
	
				# [$.data.content.node.content.isPublished=true]
				isPublished
	
				# [$.data.content.node.content.language=en]
				language
	
				# [$.data.content.node.content.editor.username=joe1]
				editor { username }
	
				# [$.data.content.node.content.edited=<not-null>]
				edited
	
				# [$.data.content.node.content.node.uuid=<is-uuid>]
				node { uuid }
			}
		}
	}
}