{
	# [$.data.contents.elements.length()=1]
	contents(query: "{\"query\":{\"query_string\":{\"query\":\"supersonic\"}}}") {
		elements {
			node {uuid}
			language
		}
	}

	# [$.data.empty.elements.length()=0]
	empty: contents(query: "{\"query\":{\"query_string\":{\"query\":\"blaar\"}}}") {
		elements {
			node {uuid}
		}
	}
}