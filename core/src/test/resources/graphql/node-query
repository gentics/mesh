{
	content(path:"/Neuigkeiten/2015/News_2015.en.html") {
		fields {
			... on content { name }
		}
		# [$.data.content.language=en]
		language

		version
		editor {username}
		edited
		node {
			# [$.data.content.node.uuid=<is-uuid>]
			uuid

			# [$.data.content.node.availableLanguages[0]=en]
			# [$.data.content.node.availableLanguages[1]=de]
			availableLanguages

			content(language: "de") {
				fields {
					... on content { name }
				}
				# [$.data.content.node.content.language=de]
				language,
				version,
				node { uuid },
				editor {username},
				edited
			},
			breadcrumb { uuid }
		}
	}
}