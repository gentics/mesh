{
	nodes(path:"/Neuigkeiten/2015") {
		uuid, 
		languagePaths(linkType: FULL) {
			languageTag, 
			link
		}, 
		availableLanguages,
		fields(language: "en") {
			... on content { title },
			... on folder { 
				name,
				nodeRef {
					uuid
				},
				binary {
					sha512sum, 
					height,
					width,
					mimeType,
					dominantColor,
					fileSize
				},
				stringList,
				dateList,
				nodeList { uuid },
				booleanList,
				numberList,
				boolean,
				number,
				date,
				htmlList,
				html
			}
		}
	}
}