{
	node(path:"/Neuigkeiten/2015") {
		uuid, 
		languagePaths(linkType: FULL) {
			languageTag, 
			link
		}, 
		availableLanguages,
		container(language: "en") {
			fields {
				... on content { title },
				... on folder { 
					name,
					nodeRef {
						uuid
						isContainer
						container(language: "en") {
							isPublished
							fields {
							... on content { title },
							... on folder { name },
							}
						}
					},
					binary {
						sha512sum, 
						height,
						width,
						mimeType,
						dominantColor,
						fileSize
					},
					stringList,
					dateList,
					nodeList { uuid },
					booleanList,
					numberList,
					boolean,
					number,
					date,
					htmlList,
					html
					micronode {
						... on vcard {
							firstName,
							lastName
						}
					}
				}
			}
		}
	}
}