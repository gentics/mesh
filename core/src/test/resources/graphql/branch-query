{
    branch {
		# [$.data.branch.uuid=<is-uuid>]
		uuid
		# [$.data.branch.name=dummy]
		name
		schema(name:"content") {
            # [$.data.branch.schema.uuid=<is-uuid>]
            uuid
            # [$.data.branch.schema.name=content]
            name
		}
		uuidSchema: schema(uuid:"70bf14ed1267446eb70c5f02cfec0e38") {
		    # [$.data.branch.uuidSchema.uuid=70bf14ed1267446eb70c5f02cfec0e38]
		    uuid
		    # [$.data.branch.uuidSchema.name=folder]
		    name
		}
		schemas {
		    elements {
                # [$.data.branch.schemas.elements[0].uuid=<is-uuid>]
		        uuid
		    }
		}
    }
}
# [$.errors=<is-undefined>]