package com.gentics.mesh.core.node;

import org.junit.Test;

import com.gentics.mesh.test.context.AbstractMeshTest;

public class NodeDeleteEndpointTest extends AbstractMeshTest {

	
	/**
	 * Test deleting the last remaining language from a node. The node is also a container which has additional child elements.
	 * Assert that the node and all subnodes are deleted.
	 */
	@Test
	public void testDeleteLastLanguageFromNode() {
		
	}
}
